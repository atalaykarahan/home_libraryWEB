export const publicRoutes = ["/", "/allbooks", "/users", "/new-verification"];

export const authRoutes = ["/login", "/register", "/reset", "/error", "/new-password"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/";
