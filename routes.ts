export const publicRoutes = [
  "/",
  "/allbooks",
  "/users",
  "/new-verification",
  "/publisher_table",
];

export const authRoutes = [
  "/login",
  "/register",
  "/reset",
  "/error",
  "/new-password",
];

export const adminRoutes = ["/profile/users"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/";
